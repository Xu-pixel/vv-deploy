export default function NotFound() {
  return (
    <main className="mx-auto flex min-h-full w-full max-w-md flex-col justify-center px-6">
      <p className="text-sm text-[var(--mute)]">没有这个页面或项目。</p>
    </main>
  );
}
