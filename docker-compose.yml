services:
  migrate:
    image: vv-deploy
    build: .
    command: ["bun", "scripts/migrate.ts"]
    user: "${VV_UID:-1000}:${VV_GID:-1000}"
    volumes:
      - ./data:/app/data
      - ./db:/app/db
    environment:
      VV_ROOT: /app
    restart: "no"

  app:
    image: vv-deploy
    build: .
    user: "${VV_UID:-1000}:${VV_GID:-1000}"
    group_add:
      - "${DOCKER_GID:-0}"
    depends_on:
      migrate:
        condition: service_completed_successfully
    ports:
      - "3003:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./repos:/app/repos
      - ./volumes:/app/volumes
      - ./overrides:/app/overrides
      - ./data:/app/data
      - ./secrets:/app/secrets
    environment:
      VV_ROOT: /app
      HOST_ROOT: ${HOST_ROOT:?set HOST_ROOT to this directory on the host}
      TRAEFIK_NETWORK: ${TRAEFIK_NETWORK:-traefik}
      VV_HOST: ${VV_HOST:-}
    restart: unless-stopped
